---
# defaults file for initial

hostname: project-name.local
packages: [
  git-core,
  curl,
  wget,
  htop,
  mc,
  build-essential,
  python-software-properties,
  python-mysqldb,
  python-psycopg2,
  unzip
]

busy_disk_space_limit: 80 # Percent of busy space after which clean up will be triggered